{{#if (eq recipient.matchedRole "agent")}}Attendance Points{{#if (eq itemCount 1)}}{{#each items}} — {{this.level}} Threshold Reached{{/each}}{{/if}}{{else}}Attendance Threshold Reached{{#if (eq itemCount 1)}}{{#each items}} — {{this.csrName}} at {{this.level}}{{/each}}{{/if}}{{/if}}
