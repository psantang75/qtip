/**
 * Re-exports shared detail primitives so existing training imports stay valid.
 * All styling is defined once in components/common/DetailLayout.
 */
export { Section, Sub, InfoRow, NoteBlock, SectionLabel, SideCard, SideTitle, ProgressRow, TopicList, ListItemReadOnly } from '@/components/common/DetailLayout'
