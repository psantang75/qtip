import PlaceholderPage from '@/pages/PlaceholderPage'

export default function ExportPage() {
  return (
    <PlaceholderPage
      title="Raw Export"
      subtitle="Export filtered datasets to Excel or CSV."
      colorClass="text-primary"
    />
  )
}
