# Copy to e2e/.env and fill passwords (e2e/.env is gitignored).
#
# 1) From repo root: npm run dev (frontend :5173 proxies /api to backend :3000).
# 2) Ensure MySQL has roles including QA; then import users from backend:
#    npx ts-node src/scripts/import-insights-users.ts
#    (from backend folder; uses backend/.env for DB). Temp password: ChangeMe123!
# 3) Set passwords below (or change in DB). Matthew Santangelo + E2E QA Auditor are added by that script.
# 4) Manager must be able to resolve Matthew's disputes (same org/department as your data model).
# 5) Quick check (~30s): npm run test:e2e:smoke
#    Full Quality UI (many minutes): npm run test:e2e:quality
#    Optional: E2E_TEST_TIMEOUT_MS=120000 for default cap on other specs

E2E_BASE_URL=http://localhost:5173

E2E_ADMIN_EMAIL=e2e-admin@dm-us.com
E2E_ADMIN_PASSWORD=

E2E_QA_EMAIL=e2e-qa@dm-us.com
E2E_QA_PASSWORD=

E2E_CSR_EMAIL=msantangelo@dm-us.com
E2E_CSR_PASSWORD=

E2E_MANAGER_EMAIL=e2e-manager@dm-us.com
E2E_MANAGER_PASSWORD=
